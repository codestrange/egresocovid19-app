import 'package:flutter/material.dart';

class Constants {
  static const appName = 'Egreso COVID-19';
  static const locales = [esLocale];
  static const enLocale = Locale('en');
  static const esLocale = Locale('es');
}
